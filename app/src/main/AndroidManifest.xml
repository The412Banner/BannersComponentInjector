<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Storage access for reading/writing component files via SAF -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Required to persist and use SAF URI grants across sessions -->
    <uses-permission android:name="android.permission.MANAGE_DOCUMENTS" />

    <!-- Required to sideload APK updates via the system installer -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <queries>
        <package android:name="gamehub.lite" />
        <package android:name="emuready.gamehub.lite" />
        <package android:name="com.tencent.ig" />
        <package android:name="com.ludashi.aibench" />
        <package android:name="com.antutu.ABenchMark" />
        <package android:name="com.mihoyo.genshinimpact" />
        <package android:name="com.xiaoji.egggame" />
        <package android:name="com.ludashi.benchmark" />
        <package android:name="com.antutu.benchmark.full" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BannersComponentInjector">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FileProvider for serving the downloaded update APK to the system installer -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
